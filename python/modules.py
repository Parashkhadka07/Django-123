#if many modules stored in one file that file called package and we have to keep that folders name mods
import time
import subprocess
for timmer in range(10,0,-1):
    subprocess.run(["clear"])
    print(timmer)
    time.sleep(1)
subprocess.run(["clear"])
print("HAPPY BIRTHDAY")